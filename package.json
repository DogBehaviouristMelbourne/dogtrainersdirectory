{
  "name": "dogtrainersdirectory",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "analyze": "vite build --mode analyze",
    "deploy": "vite build && firebase deploy --only functions",
    "deploy:pages": "vite build && echo 'Deployment to Cloudflare Pages will happen automatically when you push to GitHub'"
  },
  "dependencies": {
    "firebase": "^11.7.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.6.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "terser": "^5.26.0",
    "vite": "^6.3.5",
    "rollup-plugin-visualizer": "^5.12.0"
  }
}
